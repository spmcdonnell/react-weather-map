import React, { Component } from 'react';

class GoogleMap extends Component {
  render() {
    return (
      <main className="neighborhood-map">
        <div id="map" />
      </main>
    );
  }
}

export default GoogleMap;
